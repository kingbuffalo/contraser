package dbOper

/*
import (
	"testing"
)

func Test_getUser(t *testing.T) {
	_, err := GetUser("abc", "name", "url")
	if err != nil {
		t.Error(err)
	}
}
func Test_getPlayerInfo(t *testing.T) {
	return
	_, err := GetPlayerInfo(10, "open_id", "name", "url")
	if err != nil {
		t.Error(err)
	}

	playerInfo2, err := GetPlayerInfoBy(10)
	if err != nil {
		t.Error(err)
	} else {
		if playerInfo2.Name != "name" || playerInfo2.Url != "url" || playerInfo2.OpenId != "open_id" {
			t.Error(playerInfo2)
		}
	}
}

func BenchmarkGet(b *testing.B) {
	//for i := 0; i < b.N; i++ {
	//_, _ = GetPlayerInfo(10, "open_id", "name", "url")
	//_, _ = GetPlayerInfoBy(10)

	//}
}*/
